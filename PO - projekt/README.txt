### PROJEKT // PROJECT ###

Pliki są już skompilowane. // All files are already compiled.
    ./dc

Ponowne skompilowanie: // To compile again:
    g++ -c *.cpp
    g++ -o dc *.o
    ./dc


### DOKUMENTACJA // DOCUMENTATION ###

Strona główna dokumentacji: index.html. // Main page of documentation: index.html
Dowolny z plików html z folderu html otworzy dokumentację, ale nie na stronie głównej. // Any of the given html files opens documentation but not on the main page.